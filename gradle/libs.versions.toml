[versions]
# Новейшие проверенные версии сборщика и компилятора
agp = "9.5.0-alpha04"
kotlin = "2.4.20-RC3"

# Новейшие версии AndroidX и Compose
compose-bom = "2026.08.00"
activity-compose = "1.14.0-alpha01"

[libraries]
# Интеграция Jetpack Compose в Activity
androidx-activity-compose = { module = "androidx.activity:activity-compose", version.ref = "activity-compose" }

# Compose BOM — централизованно согласует версии компонентов UI
androidx-compose-bom = { module = "androidx.compose:compose-bom", version.ref = "compose-bom" }

# Минимально необходимые компоненты интерфейса (фон, скролл, ввод, иконки)
androidx-compose-ui = { module = "androidx.compose.ui:ui" }
androidx-compose-material3 = { module = "androidx.compose.material3:material3" }

[plugins]
# Плагины сборки
android-application = { id = "com.android.application", version.ref = "agp" }
kotlin-android = { id = "org.jetbrains.kotlin.android", version.ref = "kotlin" }
kotlin-compose = { id = "org.jetbrains.kotlin.plugin.compose", version.ref = "kotlin" }
kotlin-serialization = { id = "org.jetbrains.kotlin.plugin.serialization", version.ref = "kotlin" }